package aiagent

// CORE sub agent类型
var (
	CORE = "core"
)

// agent字典
var (
	SubAgentType = "sub_agent_type"
	CoreSubAgent = "core_sub_agent"
)

// node type
var (
	Master = "master"
	Sub    = "sub"
)
